[$writing-for-agents](/Users/jeromequeck/.agents/skills/writing-for-agents/SKILL.md)

Start a goal to build Team 63's strongest feasible autonomous competition solver by 2026-09-22 06:30 Asia/Singapore in /Users/jeromequeck/Desktop/incypher. Keep the main agent's model/reasoning defaults. Own orchestration, integration, clock checks and records; continue authorized work through reviewed PRs and merges without routine confirmation.

Read AGENTS.md, docs/context.md and docs/solver-build/brief.md. Consult docs/solver-build/research.md by workstream. Resume or initialize ignored private/solver-build-20260921/{state,plan,experiments}.md per the brief. Re-read state/plan after compaction, before dispatch and after milestones. Update checkboxes, evidence, spending, decisions, estimates and next action. Choose architecture and PR order; revise the provisional plan using results and remaining time. Preserve unmet requirements explicitly.

Optimize valid points, then finish time, within the $100 competition allowance and verified sandbox. Integrate the real runtime end to end: provider/model capability discovery and accountable adaptive budgets; resource-aware scheduling and bounded asynchronous work; compact scoped durable memory; category playbooks and specialist routing; useful lightweight tools; a clear weak-model system prompt; and autonomous timeout, crash and quiet-stall recovery. Keep unsolved challenges eligible for justified later attempts; reprioritize using progress, prior failures, value, cost, time and resource contention. Finite calls and backoff must prevent waste. Follow the brief's evidence and acceptance gates; passing helper tests alone is insufficient.

Use Luna high for local solver experiments, including the private OpenRouter configuration after verifying its actual model ID and supported reasoning request. Keep development spend separate from the competition allowance. Use Colima storage at /Volumes/Working/002 Carry/incypher colima. Parallelize isolated local experiments within host capacity; coordinate the team-wide single dynamic instance. Day 2 must honor all organiser-injected LLM settings. Treat 2026-09-22 16:30 Asia/Singapore as a soft competition spending horizon, separate from the build deadline and hard budget limits.

Delegate bounded design and implementation work to two Sol medium agents. When a PR is ready, use /Users/jeromequeck/.agents/skills/code-review/SKILL.md for independent Standards and Spec reviews by two Sol medium agents. Explicitly request model gpt-5.6-sol and reasoning medium in each child prompt and tool override; use bounded context. Schedule these four roles within actual agent-slot limits. Freeze each review's base SHA, head SHA and per-PR spec; resolve findings before merging. You own shared seams and private status files.

Keep main buildable and retain a checked image/rollback identity at each runtime merge. Run relevant regressions, the full offline suite for runtime changes, and exact-image sandbox checks. Measure solve quality, cost, latency, context and resources on matched tasks plus harder held-out cases; exclude answers/writeups from evaluation solver context.

The target includes 15/15 on the live practice board during this session, with arena-origin evidence and fresh solver validation distinguished from cumulative history. Once measured readiness supports the remaining challenges, build/check/push the Day-1 image using our private provider key and observe it hands-off; this release is authorized. Prepare a separate clean Day-2 image. Reserve time for live collection and final checks. At 06:30 freeze feature work; thereafter allow only small verified configuration fixes. Handoff exact source/image identities, checks, board evidence, costs and unresolved gaps. Mark the goal complete only when the brief's completion criteria are actually met; deadline arrival is not success.
